```
Lol look at _this_ text

That doesn't **reacts** on my tags

What a `stubborn` one 
```

// Expected result:
// <p><pre>
// Lol look at _this_ text
//
// That doesn't **reacts** on my tags
//
// What a `stubborn` one
// </pre></p>
