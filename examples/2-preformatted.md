```
Lol look at _this_ text

That doesn't **reacts** on my tags

What a `stubborn` one 
```

// Expected result:
// <pre>
// Lol look at _this_ text
//
// That doesn't **reacts** on my tags
//
// What a `stubborn` one
// </pre>
